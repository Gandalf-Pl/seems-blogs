# 仓库协作规约

本文件适用于整个仓库。后续代理在开始工作前应阅读并遵循本规约；子目录如有 `AGENTS.md`，其规则仅补充或覆盖该目录内的约定。用户当前明确要求优先于本文件，系统和开发者指令仍具有更高优先级。

## 项目定位与技术事实

- 本站是「小六子」的中文 AI 技术博客，聚焦产品实测、行业观察和工程实践。
- 使用 Jekyll 4.3 系列、Kramdown、Liquid、自定义 HTML/CSS 和原生 JavaScript；CI 使用 Ruby 3.2。
- 通过 GitHub Actions 发布到 GitHub Pages，站点地址配置为 `https://gandalf-pl.github.io/seems-blogs`，时区为 `Asia/Shanghai`。
- 当前未使用 Chirpy 主题；README 中的主题描述及占位仓库地址不能作为实现依据。技术事实以 `_config.yml`、`Gemfile`、工作流和实际代码为准。
- 延续轻量静态站点架构。仅在任务确有需要时新增依赖，不为普通页面修改引入前端框架或新的构建系统。

## 文件职责

| 路径 | 职责 |
| --- | --- |
| `_posts/` | Markdown 文章及 YAML front matter |
| `_layouts/default.html` | 全站骨架、导航、SEO、RSS、页脚 |
| `_layouts/post.html` | 文章阅读页、目录、前后篇导航、Giscus |
| `_includes/post-card.html` | 共用文章卡片 |
| `index.html` | 首页、最近更新、月度归档、搜索数据与控件 |
| `about.md` | 关于页面 |
| `assets/css/style.css` | 全站样式与响应式布局 |
| `assets/js/search.js` | 标题搜索和基于标题关键词的主题筛选 |
| `assets/js/reading.js` | 文章目录和表格滚动容器 |
| `assets/js/particles.js` | 首页背景粒子动效 |
| `_config.yml` | 站点配置、插件、永久链接、构建排除项 |
| `.github/workflows/pages-deploy.yml` | 构建与发布流程 |
| `content-plan.md` | 写作定位、栏目和篇幅参考 |

## 工作方式

- 默认用中文沟通，说明实际完成的改动、验证结果及尚存限制。
- 开始前检查 `git status --short` 并阅读相关代码；保留用户已有修改，不覆盖、回滚或顺带清理无关文件。
- 围绕当前任务做最小完整改动，沿用邻近代码风格，不批量格式化、不擅自重写历史文章。
- 文件使用 UTF-8。PowerShell 读写中文时显式指定编码，避免乱码或破坏 YAML、Liquid。
- 普通且可逆的实现细节自主处理；只有缺失信息会实质影响结果时才询问。
- 不提交密钥、令牌、私人配置、构建缓存、临时预览文件或 `_site/` 产物。
- 架构、构建命令或目录职责发生变化时，同步更新相关说明和本规约。

## 内容写作

- 新文章文件名采用 `YYYY-MM-DD-lowercase-hyphenated-slug.md`；保留已有中文文件名，避免改变已发布 URL。
- 新文章至少包含 `layout`、`title`、`date`、`categories`；需要时添加 `tags` 和 `description`。示例：

  ```yaml
  ---
  layout: post
  title: "文章标题"
  date: 2026-09-05 09:00:00 +0800
  categories: [AI观察]
  tags: [Agent, 工程实践]
  description: "用一句话说明文章价值。"
  ---
  ```

- 日期与文件名一致，明确使用 `+0800`，不通过伪造日期影响排序。未来日期文章默认可能不参与 Jekyll 构建，应按任务确认预期。
- 优先复用合适的现有分类与标签，避免同义分类不断膨胀；现有 `categories` 字符串格式无需为统一格式而批量修改。
- 正文从 `##` 开始分节，`###` 用于子节，避免重复输出文章标题的一级标题。段落简短，代码块标注语言，图片提供有意义的替代文本。
- 参考 `content-plan.md`，默认约 800 字，专业、具体、有观点；深度分析和实战教程按信息量调整。栏目日历是参考，不构成自动定时发布授权。
- 新闻、价格、融资、模型能力、人物变动、法规等时效性事实必须查证，优先引用公告、官方文档、论文和原始数据，并在相关段落附可访问来源链接。
- 区分事实、推断和个人观点；标明数据的时间、单位和统计口径。不得编造来源、引述或实测经历；不能核实时明确说明或删去该断言。
- 历史文章可作为风格参考，不能直接当作事实来源。修正文中错误时核对来源，避免无依据扩大修改范围。

## 模板与前端标准

- 保持深色终端风格，以正文可读性为先；颜色优先使用现有 CSS 变量。正文保持舒适行距、清晰层级和足够对比度。
- 站内链接及资源路径使用 Liquid `relative_url`，需要完整地址时使用 `absolute_url`，兼容 `/seems-blogs` 子路径部署。
- 保留 `{% seo %}`、`{% feed_meta %}`、语义化导航、日期和永久链接机制；不擅自更改站点验证文件、Giscus 标识及评论映射。
- 渲染标题等纯文本时使用 `escape`；输出 JSON 使用 `jsonify`，嵌入 script 的文章文本继续处理 `<`，避免提前终止标签。
- JavaScript 展示文章文本使用 `textContent` 或 DOM API，不把搜索输入或文章标题直接拼入 `innerHTML`。
- 保留渐进增强：关闭或加载失败的 JavaScript 不应阻止访问文章和归档。脚本按页面需要加载，查询元素时处理不存在的情况。
- 搜索目前仅匹配标题，主题筛选使用标题关键词；更改语义时同步修改提示文案，并验证组合筛选及空结果。
- 保持键盘可操作、可见焦点、表单标签、跳转正文链接和必要的 ARIA 状态。不要仅凭颜色表示选中状态。
- 兼容手机和桌面；代码与宽表格在自身区域滚动，避免整个页面横向溢出。
- 动画遵循 `prefers-reduced-motion`，页面不可见时暂停。背景动效不得遮挡正文或捕获点击。

## 验证要求

按改动范围执行检查，不为文案改动引入测试框架，不声称完成未执行的验证。

- 所有改动：检查 diff 和 `git diff --check`，确认范围、中文编码、链接及文件路径正确。
- 文章改动：检查 front matter、日期、标题层级、来源和站内链接；环境可用时构建，确认文章出现在预期位置。
- 模板、样式、配置改动：运行生产构建，检查相关输出。界面改动还应在手机和桌面宽度检查首页、文章页和关于页的受影响部分。
- JavaScript 改动：可用 Node.js 时对修改文件执行 `node --check <文件路径>`，并验证实际交互；语法检查不等同于浏览器测试。
- 搜索相关：验证中文和英文关键词、清空输入、主题与关键词组合、空结果及恢复默认归档。
- 阅读相关：验证目录锚点、前后篇导航、长代码、宽表格和键盘操作；动效改动验证减少动态效果设置。
- 缺少 Ruby、Bundler 或浏览器时，如实报告未完成的检查及原因；静态检查不能冒充 Jekyll 构建或视觉验收。

常用命令（在仓库根目录执行；安装依赖仅在需要时进行）：

```powershell
bundle install
bundle exec jekyll serve --host 127.0.0.1
```

本地预览地址通常为 `http://127.0.0.1:4000/seems-blogs/`。修改 `_config.yml` 后重启预览服务。

生产构建检查：

```powershell
$env:JEKYLL_ENV = 'production'
bundle exec jekyll build --baseurl /seems-blogs
git diff --check
```

## 提交与发布

- 创建或修改文件不等于要求发布。用户要求提交、推送或发布时按已授权范围执行，不对同一授权重复确认。
- 推送到 `main` 会触发 Pages 构建与部署，执行前检查 diff、分支和提交内容；只暂存本任务相关文件。
- 不擅自强制推送、重写提交历史、删除分支或清除用户修改。
- 提交说明简洁描述实际变化。涉及发布时，检查对应提交的 Actions 结果和线上受影响页面；推送成功不能直接表述为部署成功。
- 交付时说明改动文件、验证结果和是否提交/发布，明确任何阻碍完成的实际问题。
